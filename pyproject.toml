[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "job-radar"
version = "0.1.0"
description = "A modular job aggregation and notification system."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]